# Normalize line endings: store LF in the repo.
* text=auto

# Shell scripts must stay LF even on Windows checkouts (bash breaks on CRLF).
*.sh text eol=lf

# Windows-only files
*.bat text eol=crlf

# Never touch these
*.png binary
*.jpg binary
*.ico binary
